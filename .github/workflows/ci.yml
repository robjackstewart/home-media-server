name: Home Media Server CI

on:
  - pull_request

env:
  tag: ${GITHUB_RUN_ID}-${GITHUB_RUN_NUMBER}

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - name: Install node
        uses: actions/setup-node@v6
        with:
          node-version: 24
      - name: Install devcontainer CLI
        run: npm install -g @devcontainers/cli

      - name: Start dev container
        run: devcontainer up --workspace-folder .

      # Note: the full `task environment:check` is deliberately not run here. It now includes
      # k3s:environment:check, which requires a running k3s service and /dev/nvidia0 - neither
      # of which exists on a GitHub-hosted runner. Those are checked on the self-hosted runner
      # at deploy time instead.
      - name: Check helm environment
        run: devcontainer exec --workspace-folder . -- task helm:environment:check

      - name: Check infrastructure environment
        run: devcontainer exec --workspace-folder . -- task infrastructure:environment:check

      - name: Check helm dependency compatability
        run: devcontainer exec --workspace-folder . -- task helm:dependencies:compatability:check
