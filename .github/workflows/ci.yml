name: Home Media Server CI

on:
  - push

jobs:
  lint:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Helm Lint
        run: docker run --rm -v ${PWD}/helm:/mount -w /mount alpine/helm lint --values sample.values.yaml --namespace home-media-server
